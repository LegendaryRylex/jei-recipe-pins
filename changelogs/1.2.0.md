# Changed

- Pin show/hide button, moved beside the open GUI instead of beside JEI's lookup history, and rebuilt on the public JEI API so the mod no longer patches JEI internals.
- Default gap above the first pin, raised clear of JEI's navigation row so recent JEI builds no longer relocate the bookmark list every time a pin moves.
